<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
</head>

<body style="font-size:20px; padding:50px;">
    <header>
        <% include ./partials/_header.ejs %>
    </header>
    <br>
    <h3>ShortURL:
        <% if (shortURL) {%>
        <b>
            <a href='/u/<%= shortURL%>'><%= shortURL%></a></b>
        <% }%>
    </h3>
    <% if (shortURL) {%>
    <% let eURL = `/urls/${shortURL}?_method=PUT`%>
    <form class="form-inline" action='<%= eURL%>' method="POST">
        <div class="form-group mb-2">
            <input class="form-control" type="text" name="longURL" placeholder="http://" value='<%= longURL%>' style="width: 300px"> &nbsp;
            <button type="submit" class="btn btn-primary">Update</button>
        </div>
    </form>
    <br>
    <br>
    <br>
    <div>
        <% let uniqueVisits = unique;%>
        <% let numVisits = visits.length;%>
        <h4><%= numVisits%> Clicks &nbsp;-&nbsp; <%=uniqueVisits%> Unique Visitors</h4>
        <table class="table table-striped" style='max-width:40%'>
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Time</th>
                    <th scope="col">ID</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < numVisits; i++) {%>
                    <tr>
                        <th scope ="row"><%= i + 1%></th>
                        <td><%= visits[i].date%></td>
                        <td><%= visits[i].id%></td>
                    </tr>
                <%}%>
            </tbody>
        </table>
    </div>
    <% }%>
</body>

</html>